<!DOCTYPE html>
<html>
<head>
    <title>Search Home</title>
    <script src="/public/validation.js"></script>
</head>
<body>
    <h1>Search Page</h1>
    <% if (error) { %>
        <p style="color:red;">Invalid input detected. Please try again.</p>
    <% } %>
    <form action="/result" method="POST" onsubmit="validateForm(event)">
        <input type="text" id="searchTerm" name="searchTerm" value="<%= value %>" required />
        <button type="submit">Search</button>
    </form>
</body>
</html>
