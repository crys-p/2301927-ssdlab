<!DOCTYPE html>
<html>
<head>
    <title>Search Home</title>
</head>
<body>
    <h1>Search Page</h1>
    <% if (error) { %>
        <p style="color:red;">Invalid input detected. Please try again.</p>
    <% } %>
    <form action="/search" method="POST">
        <input type="text" name="searchTerm" value="<%= value %>" required />
        <button type="submit">Search</button>
    </form>
</body>
</html>
